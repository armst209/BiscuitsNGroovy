// import { Snackbar, Alert as MUIAlert } from "@mui/material";

const Alert = ({
  open,
  autoHideDuration,
  onClose,
  message,
  action,
  severity,
  anchorOrigin,
  sx,
}) => {
  return (
    // <Snackbar
    //   open={open}
    //   autoHideDuration={autoHideDuration}
    //   onClose={onClose}
    //   action={action}
    //   anchorOrigin={anchorOrigin}>
    //   <MUIAlert onClose={onClose} severity={severity} sx={sx}>
    //     {message}
    //   </MUIAlert>
    // </Snackbar>
    <></>
  );
};

export default Alert;
