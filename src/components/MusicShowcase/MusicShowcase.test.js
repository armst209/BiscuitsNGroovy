import useFetch from "../../customHooks/Fetch/useFetch";

describe("The Music Showcase Component", () => {
  test("returns correct release data", async () => {
    const gen = useFetch("https://jsonplaceholder.typicode.com/todos");
  });
});
